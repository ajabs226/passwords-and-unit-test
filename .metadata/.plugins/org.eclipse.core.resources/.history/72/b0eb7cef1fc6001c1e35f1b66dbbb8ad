
public class PasswordsAndUnitTest {

	public static void main(String[] args) {

		// new password
		// 7-12 length
		// 1 number
		// no number 6
		// no spaces
		// 2 capital vowels
		// admin is an acceptable password
		// mod is an acceptable password

	}

	public static boolean addPassword(String password) {
		int six = Integer.parseInt("6");

		if (password.length() >= 7 && password.length() <= 12
				&& !(password.contains("6")) && password.contains("[A-Z].*[A-Z]") ) {
			System.out.println(password);
			return true;
		}
		
		/*
		 * if(!(password.contains("6"))) { return true; }
		 */


		return false;
	}
}
